// String Methods

// String concatenate
var name = "Vishal";
var lname = "Rajput";
console.log(name + lname);

// Template(We use `` when we have to use '' & "" inside it.)
var temp = `${name} ${lname} is a "Programmer".He is also starting 'development' work.`;
console.log(temp);

// String length
var len = name.length;
console.log(`Length of name is ${len}.`);

// Backslash Character
console.log("Hello \t World\nProgrammers");

// Contructure
var y = new String("This")
console.log(y);

document.getElementById('container').innerHTML = '<h3> Hi </h3>';